"""
ZenAI — Search Package

Public API:
    from app.search.search import search, search_exact, search_semantic
    from app.search.search import rebuild_index, index_single_entity
    from app.search.indexer import load_or_rebuild
"""
